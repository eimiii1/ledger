export const metadata = {
    title: 'Login your account | Ledger',
    description: ''
}

export default function LoginLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}