export const metadata = {
    title: 'Create your account | Vault',
    description: ''
}

export default function RegisterLayout({ children }) {
    return (
        <div
        >
            {children}
        </div>
    )
}